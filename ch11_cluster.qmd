# Cluster anlaysis

Cluster analysis was not in the original tidymodels framework. `{parsnip}` is for supervised learning like regression and classification. Instead, new package `{tidyclust}` came to provide a tidy, unified interface to clustering models.

```{r}
library(tidyclust)
```

